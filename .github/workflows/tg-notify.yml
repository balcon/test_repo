name: tg-notify
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Update status
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TELEGRAM_TO }}
        token: ${{ secrets.TELEGRAM_TOKEN }}
        format: markdown
        message: Commit in *${{ github.repository }} by _${{ github.actor }}".
          [${{github.sha}}@${{ github.ref }}]
